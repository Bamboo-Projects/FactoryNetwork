{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "Manifold",
  "scopeName": "source.manifold",
  "patterns": [
    { "include": "#comment" },
    { "include": "#string" },
    { "include": "#escaped-name" },
    { "include": "#selector" },
    { "include": "#duration" },
    { "include": "#number" },
    { "include": "#declaration" },
    { "include": "#control" },
    { "include": "#worker" },
    { "include": "#display" },
    { "include": "#source" },
    { "include": "#constant" },
    { "include": "#name-pattern" },
    { "include": "#call" },
    { "include": "#operator" }
  ],
  "repository": {
    "comment": {
      "name": "comment.line.double-slash.manifold",
      "match": "//.*$"
    },
    "string": {
      "comment": "Ohne Escapes: Der Lexer beendet den Text am ersten Anführungszeichen, also darf hier keines mittendrin durchgehen",
      "name": "string.quoted.double.manifold",
      "begin": "\"",
      "end": "\""
    },
    "escaped-name": {
      "comment": "`for` — ein Connector, der wie ein Schlüsselwort heißt. Steht vor allen Wortregeln, sonst färbt sich der Inhalt als Schlüsselwort",
      "name": "variable.other.escaped.manifold",
      "match": "`[^`]*`"
    },
    "selector": {
      "comment": "item:iron_ingot, tag:c/ingots, ofen_* als Muster — und power allein, weil Strom keine Sorte hat. Der Namensraum steht vor einem Schrägstrich, nicht vor einem zweiten Doppelpunkt",
      "name": "support.constant.selector.manifold",
      "match": "\\b(item|fluid|chemical|tag):[A-Za-z0-9_./*-]+|\\bpower\\b"
    },
    "duration": {
      "comment": "Die Einheit gehört zur Zahl: 5t, 30s, 2min, 1h",
      "name": "constant.numeric.duration.manifold",
      "match": "\\b[0-9]+(\\.[0-9]+)?(t|s|min|h)\\b"
    },
    "number": {
      "name": "constant.numeric.manifold",
      "match": "\\b[0-9]+(\\.[0-9]+)?\\b"
    },
    "declaration": {
      "comment": "Was auf oberster Ebene stehen darf",
      "name": "keyword.declaration.manifold",
      "match": "\\b(fn|worker|group|multiblock|event|display|on|import|global)\\b"
    },
    "control": {
      "name": "keyword.control.manifold",
      "match": "\\b(if|else|for|in|while|break|continue|return|let|move|except|emit|await|where|timeout|sleep)\\b"
    },
    "worker": {
      "comment": "Die Wörter, die einen Worker beschreiben",
      "name": "keyword.other.worker.manifold",
      "match": "\\b(from|to|filter|maintain|rate|per|when|priority|strategy|overflow|members|devices)\\b"
    },
    "display": {
      "comment": "Die Zeilenarten einer Anzeige",
      "name": "entity.name.tag.display.manifold",
      "match": "\\b(title|row|text|progress|indicator|list|button)\\b"
    },
    "source": {
      "comment": "Woher ein Wert kommt — storage.count, network.energy",
      "name": "support.class.manifold",
      "match": "\\b(storage|crafting|world|network|workers|multiblocks)\\b"
    },
    "constant": {
      "name": "constant.language.manifold",
      "match": "\\b(true|false|it)\\b"
    },
    "name-pattern": {
      "comment": "ofen_* in members — der Stern gehört zum Namen und ist hier kein Malzeichen",
      "name": "variable.other.pattern.manifold",
      "match": "\\b[A-Za-z_][A-Za-z0-9_]*\\*"
    },
    "call": {
      "name": "entity.name.function.manifold",
      "match": "\\b[A-Za-z_][A-Za-z0-9_]*(?=\\s*\\()"
    },
    "operator": {
      "name": "keyword.operator.manifold",
      "match": "(==|!=|<=|>=|&&|\\|\\||[+\\-*/%<>=!])"
    }
  }
}
